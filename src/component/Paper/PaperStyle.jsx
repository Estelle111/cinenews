import { navColor } from '../../style/constant'

const PaperStyle = () => ({
    PaperWrap: {
        backgroundColor: navColor,
        opacity: 0.9,
        width: '100%',
        height: '100vh',
        padding: 30,
        marginTop: 64,
        zIndex: 1000,
        position: 'relative',
    }
})

export default PaperStyle